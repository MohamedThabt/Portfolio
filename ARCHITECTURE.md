# 🏗️ Architecture Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        VISITOR'S BROWSER                         │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  GitHub Pages (Static Hosting)                          │    │
│  │  https://yourusername.github.io/Portfolio               │    │
│  │                                                          │    │
│  │  ┌──────────────────────────────────────────────┐      │    │
│  │  │  React App (App.tsx)                          │      │    │
│  │  │  - Renders portfolio                          │      │    │
│  │  │  - Calls notifyVisitor() on mount            │      │    │
│  │  └──────────────┬───────────────────────────────┘      │    │
│  │                 │                                        │    │
│  │                 ▼                                        │    │
│  │  ┌──────────────────────────────────────────────┐      │    │
│  │  │  Visitor Notifier (visitorNotifier.ts)       │      │    │
│  │  │  - Checks sessionStorage                     │      │    │
│  │  │  - Collects visitor data                     │      │    │
│  │  │  - Sends POST request                        │      │    │
│  │  └──────────────┬───────────────────────────────┘      │    │
│  └─────────────────┼──────────────────────────────────────┘    │
└────────────────────┼───────────────────────────────────────────┘
                     │
                     │ POST /api/notify
                     │ {userAgent, referrer, timestamp, language}
                     │
                     ▼
┌─────────────────────────────────────────────────────────────────┐
│                    VERCEL (Serverless)                           │
│                    https://your-project.vercel.app               │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Serverless Function (/api/notify.ts)                  │    │
│  │                                                          │    │
│  │  1. Receives POST request                              │    │
│  │  2. Validates request                                   │    │
│  │  3. Gets IP from headers                               │    │
│  │  4. Formats message                                     │    │
│  │  5. Calls Telegram API                                 │    │
│  │                                                          │    │
│  │  Environment Variables:                                 │    │
│  │  - TELEGRAM_BOT_TOKEN                                  │    │
│  │  - TELEGRAM_CHAT_ID                                    │    │
│  └──────────────┬─────────────────────────────────────────┘    │
└─────────────────┼──────────────────────────────────────────────┘
                  │
                  │ POST https://api.telegram.org/bot{TOKEN}/sendMessage
                  │ {chat_id, text, parse_mode}
                  │
                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                    TELEGRAM API                                  │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  Telegram Bot API                                       │    │
│  │  - Validates bot token                                  │    │
│  │  - Sends message to chat                               │    │
│  │  - Returns success/error                               │    │
│  └──────────────┬─────────────────────────────────────────┘    │
└─────────────────┼──────────────────────────────────────────────┘
                  │
                  │ Push Notification
                  │
                  ▼
┌─────────────────────────────────────────────────────────────────┐
│                     YOUR TELEGRAM                                │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐    │
│  │  💬 Bot Message                                         │    │
│  │                                                          │    │
│  │  🌐 New Visitor Alert!                                 │    │
│  │                                                          │    │
│  │  ⏰ Time: 11/15/2025, 3:45 PM                         │    │
│  │  📱 User Agent: Mozilla/5.0...                        │    │
│  │  🔗 Referrer: https://google.com                      │    │
│  │  🌍 Language: en-US                                   │    │
│  │  🖥️ IP: 203.0.113.42                                 │    │
│  └────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

### Step 1: Visitor Opens Site
```
Browser → GitHub Pages → Load React App
```

### Step 2: App Initialization
```
React App → useEffect() → notifyVisitor()
```

### Step 3: Check Session
```
notifyVisitor() → sessionStorage.getItem('visitor_notified')
├─ If found → Exit (already notified)
└─ If not found → Continue
```

### Step 4: Collect Data
```javascript
{
  userAgent: "Mozilla/5.0...",
  referrer: "https://google.com",
  timestamp: "2025-11-15T15:45:30.123Z",
  language: "en-US"
}
```

### Step 5: Send to API
```
POST https://your-project.vercel.app/api/notify
Content-Type: application/json
Body: {visitor data}
```

### Step 6: Process Request
```
Vercel Function
├─ Validate method (POST only)
├─ Get IP from headers
├─ Format message with emojis
└─ Call Telegram API
```

### Step 7: Deliver Notification
```
Telegram API → Your Telegram App → 🔔 Notification!
```

### Step 8: Mark as Notified
```
sessionStorage.setItem('visitor_notified', 'true')
```

## 🔐 Security Layers

```
┌─────────────────────────────────────────────────────────┐
│  Layer 1: Environment Variables                          │
│  - Secrets stored in Vercel                             │
│  - Not in code or repository                            │
└─────────────────────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 2: CORS Protection                               │
│  - Only allows POST requests                            │
│  - CORS headers configured                              │
└─────────────────────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 3: HTTPS Only                                    │
│  - All communication encrypted                          │
│  - No plain text transmission                           │
└─────────────────────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 4: Session-Based                                 │
│  - One notification per session                         │
│  - Prevents spam/abuse                                  │
└─────────────────────────────────────────────────────────┘
                      ▼
┌─────────────────────────────────────────────────────────┐
│  Layer 5: No Data Storage                               │
│  - Serverless (stateless)                               │
│  - No database                                          │
│  - No visitor tracking                                  │
└─────────────────────────────────────────────────────────┘
```

## 📊 Component Breakdown

### Frontend (React)
```typescript
App.tsx
├─ useEffect hook
│  └─ Calls notifyVisitor() on mount
│
src/lib/visitorNotifier.ts
├─ Checks sessionStorage
├─ Collects browser data
├─ Sends POST request
└─ Marks session as notified
```

### Backend (Vercel)
```typescript
api/notify.ts
├─ Handles POST /api/notify
├─ Validates request
├─ Formats message
├─ Calls Telegram API
└─ Returns success/error
```

### Configuration
```
.env (Local)
├─ TELEGRAM_BOT_TOKEN
├─ TELEGRAM_CHAT_ID
└─ VITE_NOTIFY_API_URL

Vercel Environment
├─ TELEGRAM_BOT_TOKEN (secret)
└─ TELEGRAM_CHAT_ID (secret)

vercel.json
└─ Function routing configuration
```

## 🚀 Deployment Flow

```
Local Development
       │
       ▼
┌──────────────┐
│ git push     │
└──────┬───────┘
       │
       ▼
┌─────────────────────┐
│ Vercel Deployment   │
│ - Build functions   │
│ - Set env vars      │
│ - Deploy to CDN     │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ GitHub Actions      │
│ - Build React app   │
│ - Deploy to Pages   │
└──────┬──────────────┘
       │
       ▼
┌─────────────────────┐
│ Live Site!          │
│ GitHub Pages +      │
│ Vercel API          │
└─────────────────────┘
```

## 💡 Why This Architecture?

### ✅ Advantages

1. **Serverless**
   - No server to maintain
   - Auto-scaling
   - Pay per use (free tier)

2. **Decoupled**
   - Frontend on GitHub Pages
   - Backend on Vercel
   - Easy to update independently

3. **Secure**
   - Secrets in environment variables
   - HTTPS everywhere
   - No data storage

4. **Fast**
   - CDN-served static files
   - Edge functions (low latency)
   - Minimal JavaScript

5. **Free**
   - GitHub Pages: Free
   - Vercel: Free tier
   - Telegram: Free

### 🎯 Alternative Architectures

#### Option 1: All Vercel
```
Vercel (Static + Functions)
└─ Simpler, but tied to one platform
```

#### Option 2: Netlify
```
Netlify (Static + Functions)
└─ Similar to Vercel approach
```

#### Option 3: AWS Lambda
```
S3 (Static) + Lambda (Functions) + API Gateway
└─ More complex, but more control
```

#### Option 4: Self-Hosted
```
VPS + Node.js Server
└─ Full control, but maintenance required
```

---

**Current Architecture Choice:** GitHub Pages + Vercel
**Reason:** Best balance of simplicity, cost, and features
